[project]
name = "wordler"
version = "0.1.0"
description = "Bot to play wordle!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic==2.10.6",
    "pydantic-settings==2.7.1",
]
